import 'package:flutter/material.dart';
import 'package:myapp/features/filtering/presentation/widgets/filtering_body.dart';

class filtering_view extends StatelessWidget {
  const filtering_view({super.key});

  @override
  Widget build(BuildContext context) {
    return filtering_body();
  }
}
